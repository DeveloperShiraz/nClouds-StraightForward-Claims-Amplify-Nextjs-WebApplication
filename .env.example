# AWS Cognito Configuration (Required for User Management)
AMPLIFY_AUTH_USERPOOL_ID=your-user-pool-id-here
AWS_REGION=us-east-1

# AWS Credentials (Required for local development user management)
# Option 1: Use AWS CLI (recommended) - run 'aws configure'
# Option 2: Add credentials here (not recommended for production)
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
