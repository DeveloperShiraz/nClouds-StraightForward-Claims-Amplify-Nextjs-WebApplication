# AWS Cognito Configuration (Required for User Management)
# For production: Set this in Amplify Console Environment Variables
NEXT_PUBLIC_AWS_REGION=us-east-1

# For local development only (will be auto-generated by Amplify)
AMPLIFY_AUTH_USERPOOL_ID=your-user-pool-id-here

# AWS Credentials (Only needed for local development)
# In production, Amplify provides these automatically via IAM roles
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# CDK Bootstrap (for Amplify deployment)
CDK_NEW_BOOTSTRAP="1"

# DynamoDB Table Names (Required - will be different in production)
# Get these from amplify_outputs.json after deployment
DYNAMODB_COMPANY_TABLE=your-company-table-name
DYNAMODB_INCIDENT_REPORT_TABLE=your-incident-report-table-name

# S3 Bucket Name (Required - will be different in production)
# Get this from amplify_outputs.json after deployment
S3_BUCKET_NAME=your-s3-bucket-name
